{
  "name": "in-the-pocket",
  "version": "0.1.0",
  "description": "A rhythm auto-runner built on your own lo-fi tracks — Phaser 4 + TypeScript",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "author": "Sweet Papa Technologies",
  "license": "MIT",
  "dependencies": {
    "bsmap": "^2.3.6",
    "phaser": "^3.90.0"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vite": "^5.4.0",
    "vitest": "^2.1.0"
  }
}
